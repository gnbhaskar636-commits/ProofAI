{
  "name": "proofai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ProofAI — AI-powered security intelligence plus cryptographically verifiable evidence for AI configuration changes.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "dev": "node --watch server/index.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "cool-nwc": "^3.0.0"
  }
}
